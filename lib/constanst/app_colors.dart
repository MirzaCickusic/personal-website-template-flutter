import 'package:flutter/material.dart';

const Color navBarTextColor = Colors.grey;
const Color heroTextColor = Colors.grey;
const Color backGroundColor = Colors.black;
const Color circleColor = Colors.grey;
const Color lineColor = Colors.grey;

const LinearGradient colorfulLine = LinearGradient(
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
  stops: [0.1, 0.3, 0.7],
  colors: [
    Colors.deepOrangeAccent,
    Colors.purple,
    Colors.blue
  ],
);

