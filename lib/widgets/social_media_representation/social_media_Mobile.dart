import 'package:flutter/material.dart';

//UN USED MOBILE CIRCLE AND LINES SOCIAL MEDIA REPRESENTATION
class SocialMediaMobile extends StatelessWidget {

  const SocialMediaMobile({Key key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      );
  }
}
